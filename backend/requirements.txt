# Backend dependencies for the RagFabric.

# Web framework + server
fastapi
uvicorn[standard]
python-multipart          # multipart/form-data parsing for file uploads

# Offline-first embeddings + vector math (no model download, no network)
numpy

# Document parsers (PDF, DOCX, PPTX). TXT/CSV use the stdlib.
pypdf
python-docx
python-pptx

# Database
sqlalchemy
alembic                  # schema migrations (see alembic/ and `alembic upgrade head`)
psycopg2-binary           # PostgreSQL driver (used in Docker; SQLite needs nothing)

# Auth
python-jose[cryptography] # JWT encode/decode
bcrypt                    # password hashing

# Config / validation
pydantic
pydantic-settings
email-validator           # validates EmailStr fields on the auth schemas

# --- dev / test only ---
pytest
httpx                     # FastAPI TestClient transport

# --- optional online upgrades (NOT required for the default offline path) ---
# sentence-transformers   # real semantic embeddings
# qdrant-client           # managed vector DB backend
# anthropic               # generated (non-extractive) answers
